// @flow

// node_modules
import { css } from "styled-components";

// Constants
import { Measurements } from "Base/Constants";

export default css`
    min-width: 1px;
    display: block;
    min-height: ${Measurements.headerHeight};
`;
