// @flow

// node_modules
import { css } from "styled-components";

export default css`
    && {
        flex-direction: row;
        flex-wrap: wrap;
        justify-content: space-between;
        width: 100%;
        justify-content: center;
        align-items: center;
    }
`;
